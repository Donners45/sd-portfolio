import React from 'react';
import styles from './pageBrake.module.scss';

function PageBrake() {
    return (
        <span className={styles.pageBrake}>
        </span>
    );
}

export default PageBrake;